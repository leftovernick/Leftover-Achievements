# Install as /etc/sudoers.d/leftover-achievements-update with mode 0440.
# Replace YOUR_USER with the non-root account running the application service.
YOUR_USER ALL=(root) NOPASSWD: /usr/bin/systemctl restart leftover-achievements.service
